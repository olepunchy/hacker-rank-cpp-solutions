#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

class Difference {
    private:
    vector<int> elements;

    public:
        Difference(vector<int> &elements): elements(elements) {};
        void computeDifference() {

        };

        int maximumDifference;

};

int main() {
    // Do something
    return 0;
}
